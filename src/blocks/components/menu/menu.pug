include menu.data.pug

//- Основное меню в header
mixin menu(menuList)
    if !menuList
        - menuList = menu
    nav.menu
        ul.menu__list
            for item in menuList
                li.menu__item
                    a(href='#').menu__link=item.name
